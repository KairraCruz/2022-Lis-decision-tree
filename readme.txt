Running the script:

    1. Install at least Python 3.6
    2. Install requirements using command:

    $ pip install -r requirements.txt

    3. Run the script:

    $ python main.py


To change the model:

    Edit fit_to_model() function